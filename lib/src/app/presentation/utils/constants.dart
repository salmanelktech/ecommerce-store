import 'package:flutter/material.dart';

class AppColors {
  static const Color primaryColor = Color(0xFF4CAF50); // Green
  static const Color backgroundColor = Colors.white;
  static const Color inputFillColor = Color(0xFFF0F0F0); // Light Grey
  static const Color errorColor = Colors.red;
  static const Color facebookColor = Colors.blue;
  static const Color greyShade700 = Color(0xFF616161); // Grey Shade 700
}

//* ----------------- Padding Constants ------------------
class AppPaddings {
  static const EdgeInsets horizontalPadding =
      EdgeInsets.symmetric(horizontal: 24.0);
}

//* ------------------ DEFAULT_PADDING ------------------

const double kPadding = 8;
const double kVerticalPadding = 80;

//* ------------------ DEFAULT_RADIUS ------------------

const double kRadius = 8;

//* ------------------ KEYS ------------------